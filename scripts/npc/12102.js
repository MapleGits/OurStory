function start(mode, type, selection) {
	    if (job == 100 || job == 200 || job == 300 || job == 400 || job = 500) {
            cm.maxSkillsByJob();
            cm.dispose();
	    } else {
		cm.sendSimple("You are not an explorer");
		}
}