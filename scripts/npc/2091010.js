importPackage(Packages.tools.packet);
function start() {
    //cm.getPlayer().getClient().getSession().write(CWvsContext.getMulungRanking(cm.getPlayer().getClient()));
    cm.sendOk("Dojo Rankings are currently under a maintenance, please come later :)")
    cm.dispose();
}